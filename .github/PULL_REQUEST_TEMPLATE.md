## What and why

<!-- What does this change, and why is it needed? Link the issue it fixes. -->

## How it was tested

<!-- Tests added or changed, manual checks, deployment type if relevant. -->

## Checklist

- [ ] `make test vet lint` passes.
- [ ] UI changes: `npm run check` in `web/` reports no errors or warnings, and every new string exists in English and German.
- [ ] Changed behaviour is documented (`docs/API.md`, `docs/ARCHITECTURE.md`, `docs/DEPLOYMENT.md`, README), and user-visible changes have a line under "Unreleased" in `CHANGELOG.md`.
- [ ] No new Go module or npm package, or it was agreed in an issue first.
- [ ] No real passwords, tokens, personal data or addresses of your own network in code, tests, logs or screenshots.

<!-- Security vulnerabilities: do not open a pull request. Report them privately, see docs/SECURITY.md. -->
